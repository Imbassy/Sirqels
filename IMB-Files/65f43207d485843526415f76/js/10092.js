// This file was generated by Slater.app - Home.js 

/* var currentTime = new Date().toLocaleString({ timeZone: 'America/New_York' });
console.log(currentTime); // Displays the current time of New York City*/

/* function displayBelgradeTime() {
  var currentTime = new Date().toLocaleTimeString("en-US", { timeStyle: "short",
    timeZone: "Europe/Belgrade" });
  document.getElementById('#belgrade').textContent = 'Current time in Belgrade: ' + currentTime;
}

// Prikazivanje vremena odmah i ažuriranje svake sekunde
displayBelgradeTime();
setInterval(displayBelgradeTime, 1000);*/

/* function displayTime() {
  // Dobavljanje trenutnog vremena u Beogradu
  var currentTime = new Date().toLocaleTimeString("en-US", {
    timeStyle: "short",
    timeZoneName: "short",
    timeZone: "Europe/Belgrade"
  });
  $('#belgrade, #belgrade-1').html(currentTime);
}

setInterval(displayTime, 1000); // Izvršavanje displayTime funkcije svake sekunde */

/* function displayTime() {
  // Dobavljanje trenutnog vremena u BeograduFloat32Array 
  var currentTime = new Date().toLocaleTimeString("en-US", {
    timeStyle: "short",
    timeZone: "Europe/Belgrade"
  });
  $('#belgrade').text(currentTime);

  // Dobavljanje trenutnog vremena u New Yorku
  currentTime = new Date().toLocaleTimeString("en-US", {
    timeStyle: "short",
    timeZone: "America/New_york"
  });
  $('#newyork').text(currentTime);

  // Dobavljanje trenutnog vremena u Torontu
  currentTime = new Date().toLocaleTimeString("en-US", {
    timeStyle: "short",
    timeZone: "America/Toronto"
  });
  $('#toronto').text(currentTime);

  // Dobavljanje trenutnog vremena u Dubaiju
  currentTime = new Date().toLocaleTimeString("en-US", {
    timeStyle: "short",
    timeZone: "Africa/Cairo"
  });
  $('#dubai').text(currentTime);
}

setInterval(displayTime, 1000); // Izvršavanje displayTime funkcije svake sekunde */

